import { describe, expect, test } from "bun:test";

// this is just a stub test so bun test runs through. create
// your own test files and delete this one once these are in place

describe("stub test", () => {
	test("stub test", () => {
		expect(true).toEqual(true);
	});
});
